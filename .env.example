# AI
GEMINI_API_KEY=                    # Google AI Studio (bos = mock mode)

# x402
X402_MOCK=true                      # true = mock payments (default)
X402_FACILITATOR_URL=https://x402.org/facilitator
PAY_TO_ADDRESS=                     # Pet wallet adresi (OWS'den alinir)

# App
NEXT_PUBLIC_MOCK_MODE=true          # Master switch for mock mode
NEXT_PUBLIC_INITIAL_BALANCE=10      # Baslangic bakiyesi ($)
NEXT_PUBLIC_STAT_DECAY_SPEED=3      # Stat decay carpani (yuksek = hizli)
NEXT_PUBLIC_NETWORK=eip155:84532    # Base Sepolia (CAIP-2)

# OWS (sadece local demo icin)
OWS_WALLET_NAME=cryptogotchi-pet
